{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "PYTHONPATH=backend DJANGO_SETTINGS_MODULE=tennis_backend.settings python backend/manage.py collectstatic --noinput && PYTHONPATH=backend DJANGO_SETTINGS_MODULE=tennis_backend.settings python backend/manage.py migrate --noinput && PYTHONPATH=backend DJANGO_SETTINGS_MODULE=tennis_backend.settings gunicorn tennis_backend.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
