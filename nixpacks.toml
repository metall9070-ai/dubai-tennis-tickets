[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python manage.py migrate --noinput && python manage.py create_superuser_from_env && python -m gunicorn tennis_backend.wsgi:application --bind 0.0.0.0:$PORT"
